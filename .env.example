ORCAROUTER_API_KEY=sk-orca-your-key-here
ORCAROUTER_MODEL=deepseek/deepseek-v4-flash-free
ORCAROUTER_BASE_URL=https://api.orcarouter.ai/v1

# Optional Django overrides. docker-compose.yml reads these too; leave them
# unset to get the compose defaults (DEBUG off, localhost only).
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_DEBUG=True
